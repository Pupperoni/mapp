<%= form_with(model: airline_operator, local: true) do |form| %>
  <% if airline_operator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(airline_operator.errors.count, "error") %> prohibited this airline_operator from being saved:</h2>

      <ul>
      <% airline_operator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
